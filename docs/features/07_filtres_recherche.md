# Filtres et recherche

Finalité:
- Filtrer et rechercher les commandes par statut, type de livraison et zone pour prioriser les interventions.

Données affichées:
- Résultats filtrés, contrôles de filtrage dans l'UI.

Actions / Endpoints associés:
- Requêtes côté serveur via `CommandeRepository` et contrôleur `CommandeController`.

Fichiers référents:
- `src/Repository/CommandeRepository.php`, `templates/commande/list.html.twig`
